# Pre-commit hooks are centrally managed.
# This file must match the pinned standards version in .trestle/standards.version.

repos:
  - repo: local
    hooks:
      - id: standards-validate
        name: Validate standards pin and drift
        entry: bash scripts/validate_standards.sh
        language: system
        pass_filenames: false
